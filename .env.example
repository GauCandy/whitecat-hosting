# Server Configuration
PORT=3000

# Host to bind to (IPv4)
# 0.0.0.0 - Listen on all interfaces (recommended for production)
# 127.0.0.1 - Localhost only (for development)
HOST=0.0.0.0

# Environment
NODE_ENV=development

# Discord OAuth Configuration
# Tạo application tại: https://discord.com/developers/applications
# REDIRECT_URI phải là URL THẬT mà user truy cập (domain/IP trước proxy)
# Ví dụ: https://yourdomain.com/auth/discord/callback hoặc http://your-public-ip/auth/discord/callback
DISCORD_CLIENT_ID=your_discord_client_id
DISCORD_CLIENT_SECRET=your_discord_client_secret
DISCORD_REDIRECT_URI=http://localhost:3000/auth/discord/callback

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=whitecat_hosting
DB_USER=postgres
DB_PASSWORD=your_password_here
